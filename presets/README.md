# Presets

The complete five-component patch can be regenerated with:

```bash
python3 build.py "Secret of Mana (USA).sfc" all -o presets/all_five.ips
```

Generated IPS files are build outputs and are not stored in the repository.
