# French VWF intro overlay

This component is the French profile delta over `default_vwf_intro`. It owns
only the French intro metrics and encoding differences; the renderer/runtime
remains owned by the default component.
