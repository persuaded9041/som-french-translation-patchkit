# French VWF dialogue overlay

This component is the French profile delta over `default_vwf_dialogues`. It
owns the French DTE route, French glyph writes and French metric differences.
It must be applied after its default base component.
