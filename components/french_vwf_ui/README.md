# French VWF UI overlay

This component is the French profile delta over `default_vwf_ui`. It owns the
French glyph writes and metric differences; the UI VWF runtime remains in the
default component.
